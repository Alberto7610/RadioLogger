<Window x:Class="RadioLogger.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Acceso Restringido" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Border Background="#1A1A1E" BorderBrush="#00AABB" BorderThickness="1" CornerRadius="8" Padding="25">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="5" Opacity="0.8"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="&#x1F512;" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="ACCESO A CONFIGURACIÓN" Foreground="#00AABB" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock Grid.Row="1" Text="Ingrese la contraseña:" Foreground="#888" FontSize="11" Margin="0,0,0,8"/>

            <PasswordBox x:Name="PasswordInput" Grid.Row="2" Height="35" FontSize="14"
                         Background="#111" Foreground="White" BorderBrush="#333" BorderThickness="1"
                         Padding="8,5" MaxLength="50"
                         KeyDown="PasswordInput_KeyDown"/>

            <TextBlock x:Name="ErrorText" Grid.Row="3" Text="" Foreground="#FF4444" FontSize="10"
                       FontWeight="Bold" Margin="0,8,0,0" Visibility="Collapsed"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="CANCELAR" Click="Cancel_Click" Width="90" Height="32" Margin="0,0,10,0"
                        Background="#2A2A2A" Foreground="#888" BorderBrush="#333" BorderThickness="1" Cursor="Hand"/>
                <Button Content="ENTRAR" Click="Accept_Click" Width="90" Height="32"
                        Background="#0A2A2A" Foreground="#00AABB" BorderBrush="#00AABB" BorderThickness="1"
                        FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
